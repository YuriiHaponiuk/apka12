<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mstudent">

    <application
        android:allowBackup="true"
        android:label="mStudent"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MStudent">

        <activity android:name=".FormularzActivity" />
        <activity android:name=".PrawoJazdyActivity" />
        <activity android:name=".LegitymacjaActivity" />
        <activity android:name=".IdCardActivity" />
        <activity android:name=".DrivingLicenseActivity" />
        <activity android:name=".DaneActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
